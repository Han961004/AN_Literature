<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".view.sign.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project"
        tools:targetApi="31">
        <activity
            android:name=".view.sign.up.SignUpActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!--   Main 관련   -->
        <activity android:name=".view.main.MainActivity"/>


        <!--   Loading 관련   -->
<!--        <activity android:name=".view.loading.LoadingActivity"/>-->


        <!--   Sign 관련   -->
<!--        <activity android:name=".view.sign.up.SignUpActivity"/>-->
        <!--        <activity android:name=".view.activity.SplashActivity"/>-->
        <activity android:name=".view.sign.in.local.SignInActivity"/>

        <!--   Feedback 관련     -->
        <activity android:name=".view.feedback.FeedbackListActivity"/>
        <activity android:name=".view.feedback.FeedbackDetailActivity"/>

        <!--   Post 관련     -->
        <activity android:name=".view.post.write.PostWriteActivity"/>
        <activity android:name=".view.post.list.PostListActivity"/>
        <activity android:name=".view.post.detail.PostDetailActivity"/>
        <activity android:name=".view.post.comment.PostCommentActivity"/>

    </application>

</manifest>